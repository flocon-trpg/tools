import { NextPage } from 'next';

export const Index: NextPage = () => {
    return (
        <div style={{ padding: 16 }}>
            <a href="https://flocon.app/tools">https://flocon.app/tools</a> に移行しました。
        </div>
    );
};
