import { NextPage } from 'next';

export const WebServer: NextPage = () => {
    return (
        <div style={{ padding: 16 }}>
            <a href="https://flocon.app/tools/web-server">https://flocon.app/tools/web-server</a>{' '}
            に移行しました。
        </div>
    );
};
